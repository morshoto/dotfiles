# Codex Rules

Shared Codex rules can live here when they are portable across machines and
projects.

Do not commit generated allowlists, per-project command approvals, session
state, or other local-only rule files copied from `~/.codex/rules`.
