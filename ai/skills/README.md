# Shared skills

Place skills shared by Codex and Claude Code here. Home Manager symlinks this
directory to both `~/.codex/skills` and `~/.claude/skills` using out-of-store
links, so edits in this repo apply directly.
